# Prototype path and CDN integrity repairs

Owner: Alchemical PR #47. Restrict the archived synth example updateEngine path to existing own properties, rejecting empty and prototype-related segments. Preserve immutable cloning and ordinary nested updates. The source example remains in scope despite being under docs.

Three flagged HTML script references retain their exact versions and URLs, now with SHA-384 integrity and crossorigin anonymous. The companion JSON records fetched byte lengths, digests and observed wildcard CORS support. These are current CDN bytes, not a claim of independent vendor signing. Browser loading and hosted scan clearance remain separate checks.
