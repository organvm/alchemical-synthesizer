# Media shell compatibility repair

Owner: Alchemical PR #47. Narrow reproduction: the exact rebroadcast configuration block succeeded under /opt/homebrew/bin/bash and failed under /bin/bash, while the Python body alone succeeded. Replace affected f-string emission in nested command substitutions with concatenation plus shlex.quote. Quote every rebroadcast contract value and submission URL/license, preventing shell evaluation of value contents as well as avoiding the observed shell parsing failure.

Verification under host heavy admission (owner alchemical-media-repair-20260916, TTL300; each command deadline90): /bin/bash tools/rebroadcast.sh --dry-run temporary-preview.flv --mode showwaves exited0 and produced nonempty FLV. A temporary generated donor queued with cc0 was consumed by /bin/bash tools/broadcast.sh --ouroboros --segments3 --seconds1 --list-size3; exit0 and nonempty lineage entries verified. Temporary outputs removed by TemporaryDirectory. Both formerly failed smoke shards now pass; unchanged 55 green shards retain their earlier evidence. No RTMP push, external submission, or deployment occurred. Release packaging/checksum verification remains next.
